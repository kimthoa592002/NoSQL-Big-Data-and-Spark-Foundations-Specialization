## Lab MongoDB Indexing - Coursera Cloud IDE

Environment:

\- Platform: Coursera / Cognitive Class

\- Tool: Cloud IDE (browser-based)

\- Database: MongoDB

\- Access: MongoDB CLI (mongosh)

---

### 1. Select databse
use training

### 2. Create a collection name bigdata
db.createCollection("bigdata")

### 3. Insert document

use training

for (i=1;i<=200000;i++){print(i);db.bigdata.insert({"account\_no":i,"balance":Math.round(Math.random()\*1000000)})}



***+ Check if 200,000 documents have been successfully inserted.***

db.bigdata.countDocuments()



### 4. Measure the time it takes to execute a query

db.bigdata.find({"account\_no":58982}).explain("executionStats").executionStats.executionTimeMillis



### 5. Work with indexes

***+ Create an index on the account\_no field.***

db.bigdata.createIndex({"account\_no}:1)



***+ Get a list of indexes on a 'bigdata' collection.***

db.bigdata.getIndexes()



### 6. Delete an index
db.bigdata.dropIndex({"account\_no":1})
